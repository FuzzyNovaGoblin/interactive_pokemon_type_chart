import 'package:flutter/material.dart';

class EffectivenessSquare extends StatelessWidget {
  const EffectivenessSquare({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
